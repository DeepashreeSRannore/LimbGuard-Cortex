# Default environment variables (shared across all environments)
# NOTE: REACT_APP_API_URL should be set per-environment:
#   - Development: set in .env.development (http://localhost:8000)
#   - Production: set via hosting platform (e.g., Vercel dashboard)
